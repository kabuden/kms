# 핵심 시스템은 표준 라이브러리만으로 동작한다(웹/DB/오프라인 데모).
# 아래는 '온라인 실데이터'와 'LLM 추론'을 켤 때만 필요한 선택적 의존성이다.

# 실시간 시세 (SA_OFFLINE=false 일 때)
yfinance>=0.2.40

# 경제뉴스 RSS 수집 (SA_OFFLINE=false 일 때)
feedparser>=6.0.11

# LLM 예측 에이전트 (SA_USE_LLM=true 이고 ANTHROPIC_API_KEY 설정 시)
anthropic>=0.40.0
